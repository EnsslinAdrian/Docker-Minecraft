# Docker-Minecraft
A fully containerized Minecraft Java Server built for a DevSecOps training project. Includes a custom Docker image, configurable server properties via environment variables, and persistent world storage using Docker volumes.
